package flow.addline

import flow.common.InitializePage
import org.jsoup.nodes.Document

/**
 * This object represents intermediate initialize page for AddLine flow
 */
class AddLineInitPage extends InitializePage {
    AddLineInitPage(Document page) {
        super(page)
    }
}
